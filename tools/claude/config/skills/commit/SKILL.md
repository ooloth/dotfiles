---
description: Commit the changes in the working tree in a logical sequence.
---

Please commit all changes in this working tree yourself (before returning to deferring committing to me).

Use multiple commits if relevant. Divide changes thematically. For example, pair one test case with its implementation and corresponding documentation updates. Changes in a single file don't necessarily belong in one commit.

Ideally, commit in a sequence that progresses from building blocks to their integration. But do not revert and reply changes to force that sequence; just do your best to divide the changes you see into a progression that shows reviewers the components of the change.
